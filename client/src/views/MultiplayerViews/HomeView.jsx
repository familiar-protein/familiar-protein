var React = require('react');

var Router = require('react-router');
var Navigation = Router.Navigation;
var Link = Router.Link;

var MultiplayerView = React.createClass({
  
  mixins: [Router.State],

  

});

module.exports = MultiplayerView;